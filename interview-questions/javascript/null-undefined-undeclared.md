`undefined` - In JavaScript, it represents the concept of an unintentionally missing value.

`null` - In practice, null is used for intentionally missing values.

`undeclared` - Not declared by `let`, `const` or `var`.
